<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blog.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.example.blog.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="baseColumns">
        id, username, password, email, nickname, avatar, role, status, created_at, updated_at
    </sql>

    <select id="selectById" resultMap="UserResultMap">
        SELECT <include refid="baseColumns"/> FROM `user` WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT <include refid="baseColumns"/> FROM `user` WHERE username = #{username}
    </select>

    <select id="selectByEmail" resultMap="UserResultMap">
        SELECT <include refid="baseColumns"/> FROM `user` WHERE email = #{email}
    </select>

    <select id="selectAll" resultMap="UserResultMap">
        SELECT <include refid="baseColumns"/> FROM `user` ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="com.example.blog.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, email, nickname, avatar, role, status)
        VALUES (#{username}, #{password}, #{email}, #{nickname}, #{avatar}, #{role}, #{status})
    </insert>

    <update id="update" parameterType="com.example.blog.entity.User">
        UPDATE `user`
        SET email    = #{email},
            nickname = #{nickname},
            avatar   = #{avatar},
            role     = #{role},
            status   = #{status}
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE `user` SET password = #{password} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM `user` WHERE id = #{id}
    </delete>

</mapper>
